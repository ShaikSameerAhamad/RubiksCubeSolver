// lib/cube-utils.ts

/**
 * NOTE: This is a placeholder. It's better to use the getScramble() function 
 * from api.ts to get a scramble directly from the backend, ensuring it's always valid.
 * This function is here to resolve the import error in page.tsx.
 */
export function generateScrambledState(): string {
  // A simple example of a scrambled state.
  // Using the backend's /scramble endpoint is the recommended approach.
  return "DRLUUBFBRBLURRLRUBLRDDFDFFLUUFFDUDFDRRRUUUDLLLLBDBFBBB";
}
